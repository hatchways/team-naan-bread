import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  notificationAvatar: {
    marginRight: '14px',
    width: '55px',
    height: '55px',
  },
}));
export default useStyles;
