import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  notification: {
    fontSize: '12px',
    fontWeight: 600,
    color: '#000000',
  },
  notificationType: {
    fontSize: '10px',
  },
}));
export default useStyles;
